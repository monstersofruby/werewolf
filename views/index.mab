div :id => 'links' do
  @links.each do |link|
    div :class => 'link' do
      p { a link.title, :href => "/links/#{link.id}" }
      p link.description
      p {"total votes: #{link.votes}"}
      p { a "vote my ball dude!", :href => "/votes/#{link.id}" }
    end
  end
end